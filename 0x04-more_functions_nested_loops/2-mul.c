#include "main.h"

/**
 * mul - multiplies two integers
 * @a: an integer
 * @b: an integer
 * Return: mul
 */

int mul(int a, int b);
{       printf("%d\n", mul(98, 1024));
        printf("%d\n", mul(-402, 4096));
	return (a * b);
}
