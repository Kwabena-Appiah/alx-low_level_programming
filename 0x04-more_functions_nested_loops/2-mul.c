#include "main.h"

/**
 * mul - multiplies two integers
 * @a: int to be multiplied 
 * @b: int to be multiplied with
 * Return: results of the operation
 */
int mul(int a, int b);
{      
       return (a * b);
}
